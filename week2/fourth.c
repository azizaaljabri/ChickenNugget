#include <stdio.h>

int main( ) {
    //This is how to write a comment.
    /*
     This is to comment with multiple lines 
    so this is also a comment
    */
    int var ;
    printf("Insert a number: ");
    scanf("%d", &var);
    printf("var = %d\n" , var);
    return 0;
}